#include <stdio.h>
{
factorial(n);
return 0;
}
void factorial (int a)//defination of afunction
{
    int i=1,f=1;
    printf("\n inside the function factorial");
    for(i=1;i<=a;i++)

    {
        f=f*i
    }
    printf("\n factorial is %d,f")
}